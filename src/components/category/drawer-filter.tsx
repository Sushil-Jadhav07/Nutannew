import {useUI} from "@/contexts/useUI";import FilterIcon from "@/components/icons/filter-icon";import {Drawer} from "@/components/common/drawer/drawer";import motionProps from "@/components/common/drawer/motion";import FilterSidebar from "@/components/filter/filter-sidebar";import cn from "classnames";import {getDirection} from "@/utils/get-direction";interface Props {    setViewAs?: (value: boolean) => void;    isDesktop?: boolean;}const DrawerFilter: React.FC<Props> = ({ isDesktop}) => {    const {openFilter, displayFilter, closeFilter} = useUI();    const dir = getDirection('en');    return (        <>            <button                className={cn("bg-white text-brand-dark w-full flex items-center px-4 py-3 mb-1.5 text-sm font-semibold transition duration-200 ease-in-out  rounded  ",                    isDesktop ? '' : 'lg:hidden'                )}                onClick={openFilter}            >                <FilterIcon/>                <span className="ltr:pl-2.5 rtl:pr-2.5">Product Filters</span>            </button>                        {/*TODO: multiple drawer uses throughout the app is a bad practice */}            <Drawer                rootClassName={'filter-drawer'}                placement={dir === 'rtl' ? 'right' : 'left'}                open={displayFilter}                onClose={closeFilter}                                {...motionProps}            >                <FilterSidebar/>            </Drawer>        </>        );}export default DrawerFilter;